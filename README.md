# offsets.upio.dev

Basic static site scaffold for Cloudflare Pages.

## Scripts

- `npm run dev` starts a local Wrangler Pages preview.
- `npm run deploy` deploys `public/` to the `offsets-upio-dev` Pages project.
