{
  "name": "offsets.upio.dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name offsets-upio-dev"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
